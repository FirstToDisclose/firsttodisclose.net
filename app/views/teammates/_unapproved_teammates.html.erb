<% @teammates.unapproved.each do |teammate| %>
  <p>
    <div>
      <%= teammate.user.email %>
    </div>
    <%= link_to "Approve Teammate", teammate_path(teammate), method: :put %>
  </p>
<% end %>
